# Service for CPU Stress Workload
apiVersion: v1
kind: Service
metadata:
  name: cpu-stress-service
  namespace: nap-demo
  labels:
    app: cpu-stress
    app.kubernetes.io/name: cpu-stress
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  selector:
    app: cpu-stress
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
